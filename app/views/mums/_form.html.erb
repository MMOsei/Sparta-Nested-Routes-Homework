<%= form_with(model: mum, local: true) do |form| %>
  <% if mum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mum.errors.count, "error") %> prohibited this mum from being saved:</h2>

      <ul>
      <% mum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :relation %>
    <%= form.text_field :relation %>
  </div>

  <div class="field">
    <%= form.label :married %>
    <%= form.check_box :married %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
